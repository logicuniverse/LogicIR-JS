# Legacy TS/JS V1 Migration

This directory records how the old TS/JS prototype schema maps to the new schema.

The old implementation is reference evidence, not schema authority.

Add mapping notes only after the corresponding new core or feature-extension concept exists.
