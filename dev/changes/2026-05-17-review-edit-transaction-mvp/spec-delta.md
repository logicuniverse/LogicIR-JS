# Spec Delta

本文件目前是 review 工作台占位，不记录已接受变更。

## Stable Docs

- `dev/process.md`: 待 review 后决定是否加入 edit transaction review route。
- `dev/long-term-vision.md`: 待 review 后决定是否引用 transaction corpus。
- `dev/roadmap.md`: 待 review 后决定。

## Packages

- `packages/core`: 当前不修改。
- Future edit/tool package: 待 review 后决定。
- `packages/tools/*`: 当前不修改。

## Schema Surface

- `schema/`: 当前不修改。

## AI Tasks / Templates

- `ai/tasks/material-index.md`: 当前不修改；review 后按结论更新。
- `ai/templates/task`: 当前不修改。

## Source Task Status

- Promoted:
- Partially promoted:
- Needs rework:
- Archive-only:

## Compatibility Notes

- Edit transaction 是 authoring/tooling concern，不是 core object model。
- Task-local hash 和 JSON path 不是正式协议。
