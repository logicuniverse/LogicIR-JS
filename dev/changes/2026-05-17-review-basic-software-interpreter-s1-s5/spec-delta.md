# Spec Delta

本文件目前是 review 工作台占位，不记录已接受变更。人工 review 完成后，如果有
正式结论，再把实际 delta 写入对应区域。

## Stable Docs

- `dev/operational-theory.md`: 待 review 后填写。
- `dev/schema-principles.md`: 待 review 后填写。
- `dev/logicir-architecture.md`: 待 review 后填写。
- `dev/feature-catalog.md`: 待 review 后填写。
- `dev/roadmap.md`: 待 review 后填写。

## Packages

- `packages/core`: 当前不修改。
- `packages/architecture`: 当前不修改。
- `packages/features/*`: 当前不修改。
- `packages/tools/*`: 当前不修改。
- Future `packages/projectors/interpreter-plan`: 待 review 后决定。
- Future `packages/engines/software`: 待 review 后决定。

## Schema Surface

- `schema/`: 当前不修改。

## AI Tasks / Templates

- `ai/tasks/material-index.md`: 当前不修改；review 后按 promotion 结论更新。
- `ai/templates/task`: 当前不修改。

## Source Task Status

待人工 review 后填写：

- Promoted:
- Partially promoted:
- Needs rework:
- Archive-only:

## Compatibility Notes

- S1-S5 task-local type subsets 不是正式 schema。
- S1-S5 plan shapes 尚未统一为一个 interpreter-plan schema。
- S1-S5 engine paths 尚未统一为一个 formal software engine。
- Legacy evidence 不决定 schema authority。
