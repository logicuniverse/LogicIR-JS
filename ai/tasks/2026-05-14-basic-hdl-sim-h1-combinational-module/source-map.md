# Source Map

| Source | Why It Matters |
| --- | --- |
| `dev/roadmap.md` | Defines H1 as combinational module with `iverilog` smoke. |
| `dev/logicir-architecture.md` | Defines projection/execution boundaries. |
| `packages/core/src/types.ts` | Shape reference for LogicIR combinational LU, ports, connections, and external LUI targets. |
| `E:\oss-cad-suite\environment.ps1` | Local HDL verification environment. |

This task mirrors only the minimal S1/H1 subset needed to compile locally. It
does not promote task-local types as formal schema.
